<?xml version="1.0" encoding="utf-8" ?>
<Shell 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:PROY10DEMO.Views"
    x:Class="PROY10DEMO.AppShell"
    Shell.FlyoutBehavior="Disabled">

    <!-- Página inicial -->
    <ShellContent 
        Title="" 
        ContentTemplate="{DataTemplate views:LoginPage}" 
        Route="LoginPage" />

    <!-- Rutas adicionales -->
    <ShellContent 
        Title="" 
        ContentTemplate="{DataTemplate views:RegistroPage}" 
        Route="RegistroPage" />

    <ShellContent 
        Title="Panel Usuario" 
        ContentTemplate="{DataTemplate views:PanelUsuario}" 
        Route="PanelUsuario" />

    <ShellContent 
        Title="" 
        ContentTemplate="{DataTemplate views:PanelAdmin}" 
        Route="PanelAdmin" />

</Shell>